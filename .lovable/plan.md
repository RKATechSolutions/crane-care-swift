

## Update Seth Adams' PIN

Change Seth Adams' login PIN from `3456` to `7384` in the mock data.

### Technical Detail
- File: `src/data/mockData.ts`
- Change `pin: '3456'` to `pin: '7384'` for the `tech-4` (Seth Adams) entry.

